package sample;

public class Controller {
/*
Set up buttons

EventRun(ActionEvent e){
  if(e.getSource==registerButton){
    Open fields to input info for createNewAccount 
  }
   if(e.getSource==loginButton){
    Open fields to input info for login 
  }
  if(e.getSource==newTask){
    Open fields to input info for Project.addTask
  }
  if(e.getSource==newProject){
    Open fields to input info for adding new project to myProjects
  }
   if(e.getSource==logout){
    run Main.logout
  }
}

*/
}
